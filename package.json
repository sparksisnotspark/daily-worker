{
  "name": "daily-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
